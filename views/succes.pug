extends layout

block content
    section.bg-light.text-center
      h1.p-3.mb-2.bg-info.text-white#title Your message
      blockquote#DecryptText.text-secondary 
        h2 "#{decryptLInk}"
            form(action='/secrets/' +link + '?_method=DELETE', method='post')
              .form-group
                input#key.form-control(type='hidden', value='delete', name='_method')
              button#submit.btn.btn-primary.delete-data(type='submit' href="#") Delete

    script(src='/bower_components/jquery/dist/jquery.js')  
    script(src='/bower_components/bootstrap/dist/js/bootstrap.js')
